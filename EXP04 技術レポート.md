# EXP04 技術レポート：災害復興期の人流を状態空間モデルと出発・到着の両側から推定する

## 要旨

HuMob Challenge 2026では、2024年能登半島地震後の2〜3月について、日別・約2 kmグリッド別の起点―終点（Origin–Destination、以下OD）行列を推定する。本手法は、配布された人流の時系列に、避難者率、断水世帯率、降水量、最深積雪、避難所の立地情報を組み合わせ、観測されていない期間のODを再構成する。

最終採用したEXP04は、同一セル内の人流である**対角成分**と、異なるセル間を移動する**非対角成分**を分けて学習する。対角成分には、外部特徴付きの局所水準状態空間モデルを用いる。非対角成分は、出発セルごとの「流出総量×行き先への配分」と、到着セルごとの「流入総量×出発地からの配分」の2通りで推定し、復元したOD値を等重みで平均する。時系列の推定にはカルマンフィルターとRTSスムーザーを用い、欠損期間の前後の観測を利用する。

最終モデルは、**対角1モデル、出発側2モデル、到着側2モデルの計5モデル**からなる。学習は正則化付き回帰と状態推定を交互に行うEM型の反復計算であり、最終構成にCatBoostによる残差補正は含まれない。1月末と4月初めを同時に隠した14日間のローカル検証において、Combined NRMSEは **0.200814** だった。

本書は、[EXP04の実行コード](src/EXP04_run.py)、そこから利用する[EXP03のモデル実装](src/EXP03_run.py)、[保存済み評価値](experiments/EXP04/metrics.json)に基づき、最終採用手法を説明する。採用に至る候補比較の経緯ではなく、実際に提出物を生成したモデルと処理を中心に記述する。数値はローカル検証の記録であり、コンペの最終順位や非公開テストのスコアは、この実験の記録には含まれていない。

## 1. 最終手法と評価結果

### 1.1 最終的に使用した構成

| 対象 | モデル化する量 | 外部特徴群 | 最終予測への使い方 |
|---|---|---|---|
| 対角成分 | セル内人流の `log1p` | 避難者率・断水世帯率・降水量 | 元の尺度に戻した値をそのまま採用 |
| 出発側の総量 | 出発セル別の非対角流出総量の `log1p` | 避難者率・断水世帯率・降水量・積雪・避難所 | 出発側配分と乗算 |
| 出発側の配分 | 各出発セルから各到着セルへの比率 | 同上。OD両端の特徴を使用 | 非負化し、出発セルごとに合計1へ正規化 |
| 到着側の総量 | 到着セル別の非対角流入総量の `log1p` | 同上。到着セルを集約の基準とする | 到着側配分と乗算 |
| 到着側の配分 | 各到着セルへの流入に占める各出発セルの比率 | 同上。出発・到着の役割を入れ替える | 非負化し、到着セルごとに合計1へ正規化 |

ここで `log1p(x)` は $\log(1+x)$ であり、値が0でも変換できる。実装上の名称は、対角が `D5`、非対角の特徴構成が `N8`、最終的な等重み結合が `N-B` である。これらは実験内の識別名であり、層数や特徴数を意味しない。

### 1.2 1月末・4月初めの評価数値

以下は [metrics.json](experiments/EXP04/metrics.json) の `results.overall` と `results.validation` に保存された最終採用モデルの値である。**すべて小さいほど良い。**

| 評価期間 | 統合 NRMSE | 対角 NRMSE | 非対角 NRMSE |
|---|---:|---:|---:|
| **1月側**  | **0.205621** | 0.090423 | 0.320819 |
| **4月側** | **0.196006** | 0.080496 | 0.311517 |
| **14日間の全体** | **0.200814** | 0.085460 | 0.316168 |

4月初めの窓では、対角・非対角の両方で1月末の窓より誤差が小さかった。ただし、これは異なる時期の検証結果であり、「復興が進むほど必ず予測しやすい」という一般的な関係を証明するものではない。また、非対角の正規化誤差が対角より大きく、最終構成でもセル間移動の推定が主な難所として残っている。

## 2. 問題の背景と設計上の仮説

### 2.1 何を予測する課題か

1日のOD行列は、行が出発セル、列が到着セルを表す。たとえば $Y_{t,o,d}∈R$ は、日付 $t$ に出発セル $o$ から到着セル $d$ へ移動した人流の公開値である。$o=d$ の対角成分は同一セル内の人流、$o\ne d$ はセルをまたぐ人流になる。

配布データには匿名化と定数による正規化が施されている。そのため、本書の「流量」「総量」は**配布値の尺度での人流量**を指す。実人数への換算は行わない。また、記録のないODは採点上0として扱うが、匿名化された集計データでの0を、現実に誰も移動しなかったことと同一視しない。

学習に使える期間は2023年11月〜2024年1月と2024年4〜10月で、予測対象は間にある2024年2〜3月である。この設定では、予測対象より後の4月以降の観測も利用できる。そこで、**欠けている期間を前後から補完する問題**としてモデル化した。未来の観測を使えないリアルタイムの翌日予測とは、利用可能な情報が異なる。

### 2.2 仮説A：曜日性と復興の変化を分けて表現する

通常の人流には曜日ごとの繰り返しがある。一方、地震後には避難、ライフラインの停止と復旧、生活拠点の変化などにより、人流の基礎水準が時間とともに変わると考えられる。

このため、予測を次の3要素に分ける。

1. 時間とともに変わる潜在的な基礎水準。
2. 曜日と年末年始による規則的な変動。
3. 避難・断水・気象・避難所立地と対応する変動。

潜在水準にはランダムウォークを置き、復興曲線の形を事前に直線や特定の指数関数へ固定しない。外部特徴で説明しきれない緩やかな変化も、この潜在水準が受け持つ。

### 2.3 仮説B：地震前後の水準は不連続になり得る

2024年1月1日の地震は、それまでの人流の延長だけでは表しにくい大きな変化である。モデルはこの日に潜在状態をリセットし、地震前の水準をそのまま地震後へ引き継がない。

ただし、リセットするのは潜在状態とその分散である。曜日係数や外部特徴の係数は、地震前後で共通の係数として学習する。モデル全体を地震前用・地震後用に完全に分割しているわけではない。

### 2.4 仮説C：疎な移動を「総量」と「配分」に分ける

特定のODペアだけを見ると、多くの日に値が0であり、正の観測も限られる。出発セル単位に集計すれば、複数の行き先に分散していた情報を総量としてまとめられる。

たとえば、ある地域から移動する量が増えたのか、移動量は同じでも行き先が変わったのかは別の変化である。「総量×配分」という分解によって、両者を別々の時系列として学習する。

### 2.5 仮説D：出発側と到着側では捉えやすい変化が異なる

出発側で集約すると、「その地域からどれだけ移動するか」を捉えやすい。到着側で集約すると、「その地域にどれだけ人が集まるか」を捉えやすい。災害復興期には両方の変化があり得るため、同じ有向ODを両側から推定して平均する。

このとき平均するのは、同じ $o\rightarrow d$ の予測値である。$o\rightarrow d$ と $d\rightarrow o$ を同じ移動とみなす仮定は置かない。移動の方向性は最終出力まで保持する。

これらの仮説はモデル設計の動機であり、各外部特徴の因果効果を実証したという意味ではない。特に避難者率や断水率は、他の復興状況とも関連する説明変数として扱う。

## 3. 使用データと前処理

### 3.1 配布ODデータ

入力は [data/raw/humob2026-dataset.tsv](data/raw/humob2026-dataset.tsv) である。1行は1日、1列目が `YYYYMMDD`、2列目が `{出発セル: {到着セル: 値}}` というPython辞書表現になっている。実装は `ast.literal_eval()` で辞書を読み取る。

| 項目 | 使用内容 |
|---|---|
| 内部で確保する日付軸 | 2023-11-01〜2024-10-31の366日 |
| 配布ファイルの学習期間 | 2023-11-01〜2024-01-31、2024-04-01〜2024-10-31の306日分 |
| 学習期間内の `NA` | 14日 |
| 最終学習に使える観測日 | 292日 |
| ローカル検証で隠す日 | 14日 |
| ローカル検証時の学習観測日 | 278日 |
| 提出対象 | 2024-02-01〜2024-03-31のうち58日 |
| 提出から除く日 | 2024-02-02、2024-03-05 |

306日、292日、366日は異なる意味を持つ。366日分の配列を確保するのは、欠損している日にも状態遷移を定義するためである。配列上に日付があるだけでは、その日を教師データにしたことにはならない。

今回、配布TSVを読み取り専用で集計し、観測292日、CV除外後278日を確認した。非対角の候補OD数は、CV時が525、全観測日を使う最終学習時が530だった。`metrics.json` の `candidate_pairs=525` は検証時の数値である。

### 3.2 評価領域と疎な表現

グリッドIDは `y_x` 形式である。評価対象となるセルを `common.competition_metric` の境界定義から生成し、出発・到着の両方が評価領域にあるODをモデル化する。評価領域は1,476セルで、対角1,476ペア、非対角2,177,100ペアからなる。境界と採点定数の実装上の正本は [competition_metric.py](src/common/competition_metric.py) にある。

一方、正の観測がある非対角ペアは、CV時点で525ペアに限られる。そこで全ODを密な行列として学習せず、**学習期間に一度でも正の値があった有向ペアの和集合**を候補集合とする。

- 対角の配列 `diag`：`日付数 × 1,476セル`。
- 非対角の配列 `flow`：`日付数 × 候補OD数`。
- `origin[p]`、`dest[p]`：候補列 `p` の出発・到着セルの整数インデックス。
- 総量 `total`：`日付数 × 1,476セル`。
- 配分 `ratio`：`日付数 × 候補OD数`。

候補集合に含まれないODは0として出力・評価される。新しいODを生成する別の分類器や、全ペアへの確率付与は行わない。この設計は計算量を抑える一方、学習期間に現れなかった移動を予測できないという制約を持つ。

### 3.3 「記録されていないOD」と「欠損日」の扱い

実装では、次の状態を区別する。

| 状態 | 学習時の扱い |
|---|---|
| 観測日に特定のODが記録されていない | そのODの値を0とする |
| 1日全体が `NA`、または非公開テスト日 | 観測マスクをFalseとし、教師に使わない |
| CVで隠した日 | 正解を `truth` に隔離し、教師に使わない |
| 総量が0の日の配分 | 比率を定義できないので、その系列・日付を配分の学習から除外 |

欠損日も配列の初期値は0だが、`observed` と系列別 `mask` により観測更新から除外される。したがって、非公開期間の人流が0だったという教師信号は入らない。

### 3.4 実際に読み込む外部データ

外部データの読み込みは `EXP03_run.Dataset.load_features()` が担当する。EXP04が直接利用する加工済みファイルは次の5種類である。

| ファイル | 使用する内容 | 役割 |
|---|---|---|
| [population_cell_map.csv](data/processed/events/population_cell_map.csv) | `grid_id`、`area`、緯度・経度 | セルと市町の対応、気象観測所への距離計算 |
| [市町別避難者率.csv](data/processed/避難者率/市町別避難者率.csv) | 日付、市町名、避難者率 | 避難状況に対応する日変動 |
| [市町別断水率.csv](data/processed/断水率/市町別断水率.csv) | 日付、市町名、断水世帯率 | ライフライン被害・復旧に対応する日変動 |
| [避難所_セル別集計.csv](data/processed/避難所/避難所_セル別集計.csv) | `facility_count` | 避難所の立地と避難者率との交互作用 |
| [weather.csv](data/processed/weather/weather.csv) | 日付、観測所、日降水量、最深積雪、品質コード | 気象条件に対応する変動と監査情報 |

前処理全体は [src/preprocessing/README.md](src/preprocessing/README.md) に整理されている。外部資料から加工済みCSVを作る工程と、加工済みCSVからモデルの入力特徴を作る工程は別であり、`EXP04_run.py` は前者を自動実行しない。

### 3.5 避難者率・断水世帯率の作成

避難者数と断水戸数は、石川県危機管理監室の「被害情報」PDFから抽出した市町別時系列を使う。前処理では `shelter_extract.py` と `water_extract.py` がPDFを読み、`evacuation_rate.py` と `water_outage_rate.py` が震災前の人口・世帯数で正規化する。

$$
\mathrm{evac}_{t,m} = \frac{E_{t,m}}{P_m}
$$

ここで $E_{t,m}$ は日付 $t$・市町 $m$ の1次避難所避難者数、$P_m$ は同市町の2023年12月人口である。

$$
\mathrm{water}_{t,m} = \frac{W_{t,m}}{H_m}
$$

ここで $W_{t,m}$ は断水戸数、$H_m$ は同市町の2023年12月世帯数である。

対象は七尾市、珠洲市、穴水町、能登町、輪島市、志賀町、中能登町、羽咋市の8市町である。市町規模で割ることで、人数・戸数の大きい地域が常に大きな特徴値になることを抑える。

原資料の集計単位と世帯数の定義が完全には一致しない場合があり、断水世帯率が1を超える値も保持する。モデル読み込み時には監査メッセージを出すが、1へ切り詰めない。

断水について、表から市町名が消えただけでは0にしない。解消が確認できた場合は0、確認できない場合は欠損として保存する。前処理は後日のPDFに記載された解消日も参照するため、資料全体を用いた事後的な再構成である。

### 3.6 報告日から日次特徴への変換

率のCSVは公表時点の値を保持し、CSV生成時点では日次補間しない。モデル読み込み時に、各日について**その日以前で最も新しい報告**を選ぶ。

```python
pos = np.searchsorted(report_dates, model_dates, side="right") - 1
```

たとえば1月10日の次の報告が1月13日なら、1月11日・12日には1月10日の報告値を使う。次の報告が来るまで値を保持する方式であり、将来の報告値との直線補間は行わない。最終報告日以降も、最後の報告を参照する。

ここで選んだ報告そのものが欠損なら、その日の特徴も欠損のままになる。「過去にさかのぼって最後の非欠損値を探す」処理ではない。地震前は、対応する報告系列が存在する市町について避難者率・断水率を0とする。対応する市町情報のないセルは欠損として残る。

報告日、報告からの経過日数、最終報告後の持ち越しなどは内部の `audit` に記録される。ただし、EXP04はこの監査配列全体を成果物として保存していない。

### 3.7 市町とグリッドの対応

`population_cell_mapping.py` が行政区域とセルの重なりを使って対応を作る。境界セルでは重なり面積を優先し、石川県側の陸上セルや近接する沿岸セルを所定の規則で8市町へ割り当てる。遠い海上セルや富山県側のセルには、対象8市町の情報を無条件に付与しない。

市町単位の率は、その市町に対応するセルへ共通に与える。人口をセル面積に応じて按分し、セル人口を直接の説明変数にする処理は行っていない。位置情報は市町対応と観測所距離の計算に使い、緯度・経度そのものをモデルの回帰特徴には入れていない。

### 3.8 降水量・積雪の作成

気象庁の過去の気象データを `weather_csv.py` で日付×観測所の縦長CSVへ変換する。EXP04が使う気象変数は、日降水量 `precipitation_total_mm` と最深積雪 `snow_depth_max_cm` である。

モデルで扱う観測所は珠洲、輪島、門前、三井、志賀、七尾の6地点。セルと各観測所の距離を緯度・経度から球面距離で計算し、**その日・その変数について有限かつ非負の値がある最寄り観測所**の値を採用する。したがって、観測所の欠測に応じて、同じセルでも日付や変数によって採用地点が変わる。

割り当てた値には `log1p` を適用し、雨量や積雪の大きな値の影響を圧縮する。利用できる観測所がない日は欠損とする。品質コードは監査記録に残すが、現在の実装には「特定の品質コードだけを採用する」という閾値フィルターはない。

### 3.9 避難所の静的情報

施設単位の [避難所.csv](data/processed/避難所/避難所.csv) を `shelter_features.py` でセル別に集計し、登録施設数 `facility_count` を使う。施設がないセルも0件として全評価セルを含める。

この数は、閉鎖済み施設を含む**登録施設の所在数**である。その日の開設施設数、利用人数、収容率を表す値ではない。最終モデルでは、登録施設数の `log1p`、施設の有無、および避難者率との積を使用する。

外部資料の出典と加工記録は [eventsのREADME](data/processed/events/README.md)、[気象のREADME](data/processed/weather/README.md)、[避難所のREADME](data/processed/避難所/README.md) を参照されたい。人口の原表と行政区域データの出典はeventsのREADMEに記録されている。施設一覧など、これらのローカル記録だけでは原本の取得URL・取得時点を特定しきれない資料については、本書では未確認の出典情報を補っていない。

## 4. 検証設計と情報漏洩を避ける処理

### 4.1 テスト期間を挟む2つの検証窓

検証日は `common.local_error_map.validation_days()` で定義する。

```text
2023-11                 2024-01                       2024-04                 2024-10
観測 ─────── 地震 ───── 観測 ── [1/25〜1/31] ── 2〜3月 ── [4/1〜4/7] ── 観測 ───── 観測
                               検証窓           非公開     検証窓
```

2つの窓を**同時に教師データから除外し、1組のモデルで両方を予測する**。1月用モデルと4月用モデルを別々に学習する構成ではない。検証では、もともとの非公開期間に加えて両端の窓も欠損になる。さらに1月23・24日と4月8日は配布上のNAなので、この長い欠損区間に接する観測日は1月22日と4月9日になる。

この設計により、復興期の長い欠損をまたいで補完できるかを確認する。一方、正解が得られるのは欠損区間の両端に近い14日であり、非公開の2〜3月中央部の精度を直接測れているわけではない。

### 4.2 検証正解を先に分離する

`Dataset(validation=True)` は、CV日のODを `self.truth` に保存した直後に `continue` する。そのためCV日の正解は、以下の処理へ入らない。

- 学習日マスク `observed`。
- 非対角の候補ペア集合。
- 学習用 `diag`、`flow`、`total`、`ratio` の観測値。
- 評価領域外ODの曜日平均。

特徴の平均補完・標準化、配分のフォールバック比率も、学習マスクを通して計算する。検証正解を使って「存在するはずのOD」を先に候補へ追加することも避けている。

外部データについては、テスト期間を含む公開情報を利用する課題設定であり、検証日の気象・被害情報も特徴として使用する。これはOD正解の利用とは別の情報経路である。ただし、実時間運用を想定した公開時刻の厳密な再現までは行っていない。

### 4.3 CVの解釈

この検証は、固定した2窓のホールドアウトである。設定に残る `crossfit_folds=5` は、EXP03の別処理でも共有する設定値であり、最終EXP04の学習経路では5-fold CVを実行していない。

実装はこの14日をモデル選択にも使用するため、掲載スコアは選択に用いた検証スコアである。独立した最終テストスコアとしては扱わない。最終提出では採用構成を固定し、CV14日も戻した292観測日で再学習する。

## 5. ODの分解と最終予測式

以下で $t$ は日付、$o$ は出発セル、$d$ は到着セルとする。総和は出発・到着とも評価領域内の非対角ODに対して取る。

### 5.1 対角成分

$$
D_{t,o}=Y_{t,o,o}
$$

$\log(1+D_{t,o})$ を状態空間モデルで予測し、逆変換と非負化によって $\widehat D_{t,o}$ を得る。対角は独立した1モデルで扱い、後述の出発・到着平均には混ぜない。

### 5.2 出発側：流出総量×行き先配分

出発セルごとの流出総量を

$$
O_{t,o}=\sum_{d\ne o}Y_{t,o,d}
$$

とし、$O_{t,o}>0$ の日に配分を

$$
r_{t,o,d}=\frac{Y_{t,o,d}}{O_{t,o}}
$$

と定義する。総量は $\log(1+O)$、配分は元の比率の尺度で別々に学習する。予測した配分を正規化した後、

$$
\widehat Y^{F}_{t,o,d}=\widehat O_{t,o}\,\widehat r_{t,o,d}
$$

と復元する。コードでは `F` が出発側を表す。

たとえば総量の予測が10、ある行き先の配分予測が0.3なら、そのODの予測値は3である。総量の変化と行き先構成の変化を別々に扱う意味が、この積に表れている。

### 5.3 到着側：流入総量×出発地配分

到着セルごとの流入総量を

$$
I_{t,d}=\sum_{o\ne d}Y_{t,o,d}
$$

とし、$I_{t,d}>0$ の日に

$$
s_{t,d,o}=\frac{Y_{t,o,d}}{I_{t,d}}
$$

と定義する。ここでも総量は `log1p`、配分は元の比率で学習し、

$$
\widehat Y^{R}_{t,o,d}=\widehat I_{t,d}\,\widehat s_{t,d,o}
$$

と復元する。`R` は到着側を表す。分子は出発側と同じ $Y_{t,o,d}$ であり、逆方向移動 $Y_{t,d,o}$ に置き換えない。

### 5.4 最終的な結合

最終採用モデル `N-B` は、復元済みの非対角ODを固定の1:1で平均する。

$$
\widehat{Y}_{t,o,d} =
\begin{cases}
  \widehat{D}_{t,o}, & o = d, \\
  \dfrac{\widehat{Y}^{F}_{t,o,d} + \widehat{Y}^{R}_{t,o,d}}{2}, & o \ne d.
\end{cases}
$$

総量同士・配分同士を別々に平均してから掛ける処理ではない。また、日付・市町・ODペアによって重みを変える学習も行わない。

出発側単独の復元では出発セル別の配分和が1になり、到着側単独では到着セル別の配分和が1になる。ただし、両者の平均後の行合計・列合計が、それぞれ $\widehat O$、$\widehat I$ と厳密に一致する保証はない。平均後の行列に対して、行・列総量を交互に合わせるIPFなどの補正は適用していない。

## 6. 各モデルに入力する特徴量

### 6.1 曜日・年末年始

`calendar(fixed=False)` は以下の7列を生成する。

- 火曜日〜日曜日の6つのダミー変数。月曜日を基準とする。
- 12月29日〜1月3日を表す年末年始ダミー。

曜日・年末年始の係数は系列ごとに持つ。対角と総量ではセルごと、配分ではODペアごとの係数になる。一般の祝日カレンダーや月別ダミーは最終構成に含まれない。基礎水準は潜在状態が担う。

### 6.2 対角成分の外部特徴

`DIAG_GROUPS = ("evac", "water", "rain")` で指定し、対象セルの次の3種類を使う。

| 実装上の特徴名 | 内容 |
|---|---|
| `origin_evac` | 対象セルに対応する市町の避難者率 |
| `origin_water` | 同じ市町の断水世帯率 |
| `origin_rain` | 対象セルに割り当てた日降水量の `log1p` |

対角モデルは、外部特徴の共通係数に加え、セル別の係数偏差も持つ。地域全体で共通する傾向を共有しつつ、セルごとの違いを許す構成である。

### 6.3 非対角総量の外部特徴

`OFF_GROUPS = ("evac", "water", "rain", "snow", "shelter")` を使う。総量モデルの標準化前の外部特徴は5種類である。

| 特徴 | 出発側総量における意味 |
|---|---|
| `origin_evac` | 出発セルの避難者率 |
| `origin_water` | 出発セルの断水世帯率 |
| `origin_rain` | 出発セルの降水量の `log1p` |
| `origin_snow` | 出発セルの最深積雪の `log1p` |
| `shelter_evac` | 出発セルの避難者率 × `log1p(登録避難所数)` |

到着側総量では、上表の対象が物理的な到着セルになる。総量モデルの避難所特徴は、この交互作用1列であり、施設数単独・施設有無の列は追加しない。

### 6.4 非対角配分の外部特徴

出発側配分モデルでは、次の11種類を生成する。

| 特徴群 | 列数 | 内容 |
|---|---:|---|
| 避難者率 | 2 | 出発セル・到着セルの値 |
| 断水世帯率 | 2 | 出発セル・到着セルの値 |
| 降水量 | 2 | 両端の `log1p` 値 |
| 最深積雪 | 2 | 両端の `log1p` 値 |
| `dest_facility_log` | 1 | 到着セルの `log1p(登録避難所数)` |
| `dest_has_shelter` | 1 | 到着セルに登録避難所があるか |
| `dest_shelter_evac` | 1 | 到着セルの避難者率 × `log1p(登録避難所数)` |

到着側配分では、仮想的な出発・到着の役割を反転する。このため `dest_*` の避難所特徴は、元のODにおける**物理的な出発セル**の情報になる。実装上の変数名だけを見て、両モデルとも物理的な到着セルの避難所を使うと解釈しないことが重要である。

### 6.5 欠損補完と標準化

`prepare_features(raw, mask, names)` は特徴ごとに次の処理を行う。

1. 学習マスクがTrueで、かつ値が有限な要素だけを集める。
2. その平均値で欠損を補完する。
3. 元の値が欠損だったかを表す `*_missing` 列を追加する。
4. 各列を、学習マスク内の平均と標準偏差で標準化する。
5. 学習値がすべて欠損の特徴、または標準偏差が `1e-10` 未満の定数列を除く。

平均・標準偏差は、当該モデルの学習対象となる日付×系列をまとめて計算する。セルごとの個別標準化ではない。配分モデルでは、総量が0の日を除いたマスクを使うため、総量モデルと補完・標準化の統計が必ずしも一致しない。

欠損フラグの追加前は対角3種類、総量5種類、配分11種類である。実際の回帰列数は、欠損フラグの追加と定数列の除外によって決まる。

## 7. 状態空間モデルの定式化

### 7.1 観測方程式

以下では、5モデルのうち1つに注目する。系列を $j$ とし、モデル別の目的変数 $z_{t,j}$ を次のように定める。

| モデル | $z_{t,j}$ |
|---|---|
| 対角 | $\log(1+D_{t,j})$ |
| 出発側の総量 | $\log(1+O_{t,j})$ |
| 到着側の総量 | $\log(1+I_{t,j})$ |
| 出発側の配分 | $r_{t,j}$ |
| 到着側の配分 | $s_{t,j}$ |

観測方程式は

$$
\begin{aligned}
z_{t,j} ={}& \ell_{t,j}
  + c_t^{\mathsf T}a_j
  + x_{t,j}^{\mathsf T}\beta \\
 &+ \mathbf{1}_{\{\mathrm{diag}\}}\,
    x_{t,j}^{\mathsf T}\delta_j
  + \epsilon_{t,j},
\qquad \epsilon_{t,j} \sim \mathcal N(0,R).
\end{aligned}
$$

である。

| 記号 | 意味 |
|---|---|
| $\ell_{t,j}$ | 日付に応じて変わる潜在水準 |
| $c_t$ | 曜日・年末年始の7列 |
| $a_j$ | 系列ごとのカレンダー係数 |
| $x_{t,j}$ | 補完・標準化後の外部特徴 |
| $\beta$ | 1回のモデル学習内で全系列が共有する外部特徴係数 |
| $\delta_j$ | 対角モデルのみが持つセル別の外部特徴係数偏差 |
| $\mathbf{1}_{\{\mathrm{diag}\}}$ | 対角モデルなら1、それ以外なら0 |
| $R$ | 観測ノイズの分散 |

総量・配分モデルには $\delta_j$ を置かない。たとえば降水量の共通係数は、出発側総量のセル間では共有するが、対角モデルや到着側総量の係数と共有するわけではない。5回の学習は、それぞれ独立した係数と分散を持つ。

### 7.2 状態方程式

潜在水準は1日ごとのランダムウォークで表す。

$$
\ell_{t,j}=\ell_{t-1,j}+\eta_{t,j},\qquad
\eta_{t,j}\sim\mathcal N(0,Q)
$$

このモデルでは、直前の水準の周囲で次の日の水準が動く。$Q$ が大きければ水準の変化を許しやすく、$R$ が大きければ観測値からの一時的なずれを許しやすい。

$Q,R$ は、各モデルの有効な系列全体から推定する共通のスカラーである。セルやODペアごとに別々の $Q,R$ を推定しているわけではない。一方、潜在水準の時系列自体は系列ごとに持つ。空間隣接グラフを通じた状態の伝播はなく、系列間の情報共有は主に共通回帰係数と共通ノイズ分散を通じて行う。

### 7.3 地震時の状態リセット

初日と2024年1月1日には、状態の事前平均を0、事前分散を `initial_variance=1e4` に設定する。大きな分散によって、観測から水準を更新する余地を持たせる。

RTSの後向き計算は、2023年12月31日と2024年1月1日の境界をまたがない。状態ノイズ $Q$ の更新からも、この境界の差分を除外する。したがって、地震当日の急変を、普段の1日変動として $Q$ に吸収させない。

### 7.4 正則化

回帰係数にはL2正則化を加える。実装の `Regression.loss()` に対応する罰則は、概略として

$$
\mathcal P =
\lambda_c \sum_j \lVert a_j \rVert^2
+ \lambda_g \lVert \beta \rVert^2
+ \mathbf{1}_{\{\mathrm{diag}\}}\,
  \lambda_\delta \sum_j \lVert \delta_j \rVert^2.
$$

となる。採用値は $\lambda_c=1$、$\lambda_g=1$、$\lambda_\delta=20$。対角のセル別偏差を強く縮小することで、観測の乏しいセルが極端な係数を持つことを抑える。罰則の対象には、保持された欠損フラグの係数も含まれる。

回帰更新の目的関数は、観測ノイズ分散を考慮した

$$
\mathcal L_{\mathrm{reg}} =
\frac{1}{2R}
\sum_{(t,j)\in\mathcal O}
\bigl(z_{t,j}-\mu_{t,j}-b_{t,j}\bigr)^2
+ \frac{1}{2}\mathcal P.
$$

ここで $\mathcal O$ は学習に使う観測位置、$\mu_{t,j}$ は推定した状態の平均、$b_{t,j}$ は回帰部分の予測値である。このため正規方程式では、正則化行列に $R$ を掛ける。`noise_variance * penalty` は、この尺度合わせの実装である。

## 8. 学習アルゴリズムの詳細

### 8.1 学習対象系列を決める

`fit_component(data, kind, groups, fixed, train)` が1モデル分の学習を行う。`kind` は `D`、`O`、`r` のいずれかである。到着側総量も共通関数では `O` と呼ぶが、渡すDatasetが変わるため実際の内容は流入総量になる。

対角・総量は、学習期間に一度でも正の値がある系列を有効とする。配分は、学習期間に正の流量があるODを有効とし、さらに各日の分母総量が正のときだけ観測として扱う。有効でない系列の予測は0にする。

### 8.2 初期化

地震前と地震後のそれぞれについて、系列ごとの観測平均を計算し、初期水準の近似値を作る。これはEM開始時の回帰・ノイズ分散の初期化に使うものであり、その後のカルマン計算の事前平均をその値に固定する処理ではない。

1. 初期水準を目的変数から引いて回帰係数を推定する。
2. 残差二乗の平均から初期の $R$ を計算する。
3. 初期の $Q$ を $0.03R$ とする。
4. 分散には `variance_floor=1e-8` の下限を設ける。

`0.03` は初期化比率であり、学習後まで固定する $Q/R$ の比率ではない。

### 8.3 カルマンフィルター：観測を順に取り込む

回帰で説明する部分を差し引いた残差系列に対して、`smooth()` がカルマンフィルターを行う。リセット日以外では、状態の予測分散に $Q$ を加える。

観測のある日には、予測誤差とカルマンゲインを

$$
\begin{aligned}
v_t &= (z_t-\mathrm{offset}_t)-m_t^{-}, \\
F_t &= P_t^{-}+R, \\
K_t &= \frac{P_t^{-}}{F_t}.
\end{aligned}
$$

として、

$$
\begin{aligned}
m_t &= m_t^{-}+K_tv_t, \\
P_t &= (1-K_t)P_t^{-}.
\end{aligned}
$$

と更新する。ここでは系列添字を省略している。

欠損日はゲインを0とし、観測による更新をしない。前日までに推定した状態を引き継ぎ、分散だけが状態遷移に応じて増える。観測がない期間を0の教師で埋めない仕組みが、この分岐にある。

### 8.4 RTSスムーザー：後ろ側の観測も取り込む

前向き計算後、最終日から逆順に状態を修正する。RTSはRauch–Tung–Striebel smootherの略で、全期間の観測を使って各日の状態を推定する処理である。

$$
\begin{aligned}
J_t &= \frac{P_t^{f}}{P_{t+1}^{-}}, \\
m_t^{s} &= m_t^{f}+J_t(m_{t+1}^{s}-m_t^{f}), \\
P_t^{s} &= P_t^{f}+J_t^2(P_{t+1}^{s}-P_{t+1}^{-}).
\end{aligned}
$$

上付き $f$ はフィルター後、$s$ は平滑化後を表す。状態方程式の遷移係数が1なので、上式では翌日の予測平均を $m_t^f$ と書ける。実装では丸め誤差による負分散を避けるため、平滑化後分散を0以上にする。

これにより、2〜3月の潜在水準は1月以前の情報だけでなく4月以降の観測からも修正される。EXP04の「出発側／到着側」という方向と、この時間方向の「前向き／後向き」は別の処理である。出発側モデル・到着側モデルの両方で、時間方向のフィルターとスムーザーを実行する。

### 8.5 回帰係数の更新

状態推定後、$z_{t,j}-E[\ell_{t,j}]$ を目的変数として回帰係数を更新する。`Regression` は、系列別のカレンダー・対角偏差と、全系列共通の外部係数を同時に推定する。

実装はNumPyの `einsum` で、系列別の設計行列の積や、全系列の外部特徴の積をまとめて計算する。観測マスクと設計行列が反復中に変わらないため、次の行列を最初に作って再利用する。

- `gram`：系列別のローカル設計行列のグラム行列。
- `zx`：ローカル設計と共通外部特徴の交差積。
- `xx`：共通外部特徴のグラム行列。

系列別係数を先に消去するSchur complementを使い、共通係数について小さな連立方程式を解く。その後、系列別係数を復元する。巨大な全係数行列を1つ作ることを避ける構造である。

`np.linalg.inv()` が系列別の正則化済み行列を処理し、共通係数側の `symmetric_inverse()` はSciPyの `eigh()` による対称行列の固有値分解を使う。小さすぎる固有値の逆数を0とすることで不安定な反転を避け、明確な負の固有値がある場合は例外を出す。

### 8.6 観測ノイズ・状態ノイズの更新

$\mu_{t,j}=E[\ell_{t,j}]$、$V_{t,j}=\operatorname{Var}(\ell_{t,j})$、更新後の回帰部分を $b_{t,j}$ とすると、観測ノイズは

$$
\begin{aligned}
\widetilde R
  &= \frac{1}{|\mathcal O|}
     \sum_{(t,j)\in\mathcal O}
     \Bigl[(z_{t,j}-\mu_{t,j}-b_{t,j})^2+V_{t,j}\Bigr], \\
R_{\mathrm{new}} &= \max\{10^{-8},\widetilde R\}.
\end{aligned}
$$

で更新する。観測がある位置だけが和に入る。

状態ノイズは、隣接状態の共分散 $C_{t,t-1,j}$ も使い、

$$
\begin{aligned}
\widetilde Q
  &= \operatorname{mean}_{(t,j)\in\mathcal E}
     \Bigl[(\mu_{t,j}-\mu_{t-1,j})^2
       +V_{t,j}+V_{t-1,j}-2C_{t,t-1,j}\Bigr], \\
Q_{\mathrm{new}} &= \max\{10^{-8},\widetilde Q\}.
\end{aligned}
$$

で更新する。$\mathcal E$ は、有効系列における日次の状態遷移から地震のリセット境界を除いた集合である。状態は欠損日にも存在するため、こちらには欠損期間内の遷移も含まれる。

`lag` はこの隣接共分散を保持する配列で、RTSのゲインと翌日の平滑化分散から計算する。単に平滑化した平均の差だけで $Q$ を推定する構成ではない。

### 8.7 反復と停止条件

反復は次の順で進む。

```text
現在の回帰係数・Q・R
  → フィルターとスムーザーで状態の平均・分散・隣接共分散を推定
  → 状態平均を差し引いて回帰係数を更新
  → Rを更新
  → Qを更新
  → 目的関数の変化を確認
```

目的関数はフィルターで計算した観測負対数尤度に、回帰のL2罰則の半分を加えた値である。連続する反復の目的関数差が

$$
|L_k-L_{k-1}|\le10^{-5}(1+|L_{k-1}|)
$$

なら停止条件を満たしたとする。上限は200反復。終了後、最後に更新した係数・分散でもう一度状態を平滑化し、予測を作る。

保存されたCV診断では、5モデルとも200反復で `converged=False` だった。したがって最終手法は、**200反復時点の推定結果を採用した構成**として記述する。十分な数値収束や大域最適性を確認したという主張はしない。

この「学習」は、観測の確率モデルと回帰係数を数値的に推定する処理である。ニューラルネットワークの逆伝播、ミニバッチ、学習率スケジュールは使わない。

### 8.8 元の尺度への復元

対角・総量は、推定した変換後の平均 $\widehat z$ を

$$
\widehat v=\max\{\exp(\widehat z)-1,0\}
$$

で戻す。設定名 `inverse_log="median_expm1"` に対応する。対数尺度でのガウス近似から見た中央値に相当する変換であり、分散の半分を指数部に加える平均補正はしない。

極端な発散を検出するため、逆変換前の有効系列の平均が50を超えた場合は例外を出す。`Prediction.variance` は対角・総量では変換後の尺度の分散であり、元の人流尺度の予測区間に変換された値ではない。

## 9. 配分の正規化とODの復元

### 9.1 配分をそのまま使わない理由

配分モデルはガウス誤差を仮定した回帰・状態空間モデルなので、予測が負になったり、同じ出発セルからの配分の和が1にならなかったりする。そこで、`base.normalize()` を必ず通す。

出発側の場合、正規化前の予測を $\widetilde r_{t,o,d}$、学習期間で観測された候補を示す値を $S_{o,d}$ とすると、

$$
r^+_{t,o,d}=\max(\widetilde r_{t,o,d},0)S_{o,d}
$$

を作る。$\sum_d r^+_{t,o,d}>0$ なら、その和で割る。

$$
\widehat r_{t,o,d}=\frac{r^+_{t,o,d}}{\sum_{d'}r^+_{t,o,d'}}
$$

### 9.2 合計が0になった場合のフォールバック

すべての配分予測が非正になった場合は、学習期間の流量を合計して作った比率へ戻す。

$$
r^{\mathrm{fallback}}_{o,d}
=\frac{\sum_{t\in\mathcal T_{\mathrm{train}}}Y_{t,o,d}}
{\sum_{t\in\mathcal T_{\mathrm{train}}}O_{t,o}}
$$

これは日別比率の単純平均ではない。流量を期間全体で合計してから比率にするため、流量の大きい日の寄与が大きい。

到着側も同じ処理を、到着セルごとの流入総量で行う。有効な候補を持つ集約セルについて、配分和が1であることを `np.allclose(..., atol=1e-10)` で確認し、有限値・非負値であることも検査する。

### 9.3 提出対象のODへの変換

`EXP04_run.reconstruct()` は、まず `Dataset.reconstruct()` を使って対角成分と必要に応じた評価領域外ODを用意する。その際、非対角の引数にはゼロ配列を渡し、次にEXP04で完成させた非対角予測を元の候補順で辞書へ挿入する。

挿入するのは正の予測値だけで、省略したODは0を意味する。復元前には非対角配列の形状、有限性、非負性を確認する。全地域の日次総量を一定値へ合わせる追加スケーリングは行わない。

### 9.4 評価領域外のOD

出発・到着の少なくとも片方が評価領域外のODは、5モデルによる予測対象から分ける。提出ファイルには、地震後の観測日を使った曜日別平均で補完する。

$$
\widehat Y^{\mathrm{outside}}_{t,o,d}
=\frac{\sum_{u\in\mathcal A_{w(t)}}Y_{u,o,d}}{|\mathcal A_{w(t)}|}
$$

$\mathcal A_{w(t)}$ は、2024年1月1日以降で、予測日と曜日が一致する学習観測日の集合である。ある日に記録されていない当該ODは、平均の計算上0として扱われる。

CV時には検証日を含めず、最終学習時にはCV日を戻した観測で平均を作り直す。特殊セル `-1_-1` を含むODもこの側に入る。これらのODは、出発・到着とも評価領域内に限定したローカル指標には影響しない。

## 10. 実装構成とimportの役割

### 10.1 ファイル間の分担

```text
src/
├── EXP04_run.py                 実行順、到着側ビュー、OD結合、評価・提出生成
├── EXP03_run.py                 Dataset、特徴生成、回帰、状態空間学習、配分正規化
├── common/
│   ├── competition_metric.py   評価領域と公式指標の計算
│   ├── experiment_io.py        TSV/JSON/CSV出力と独自の提出形式検査
│   ├── local_error_map.py      CV日付の定義とセル別誤差図
│   ├── municipality_error.py   市町別の誤差集計
│   ├── pred_dist.py            対角・非対角の日次予測推移の図
│   ├── run_log.py              工程別の計時と実行ログ
│   └── humob2026_validator.py  配布された提出形式validator
└── preprocessing/              公開資料を加工済みCSVへ変換する処理

experiments/EXP03/metrics.json   基盤モデルの設定・再現値を確認する参照ファイル
experiments/EXP04/
├── strategy.md                 事前設計
├── report.md                   実験時の結果と解釈
├── metrics.json                採用設定、検証値、学習診断
├── .log                        実行履歴
├── outputs/
│   ├── submission.tsv
│   ├── municipality_errors.csv
│   └── candidate_municipality_errors.csv
└── figures/
    ├── local_error_map.png
    ├── EXP04_pred_dist.png
    └── direction_comparison.png
```

EXP04のモデル本体を理解するには `EXP03_run.py` も必要である。`import EXP03_run as base` によって定義済みのクラスと関数を利用するが、EXP03の `run()` は `if __name__ == "__main__"` の中から呼ばれるため、importしただけでEXP03の実験全体が開始されることはない。

EXP04は `base.OUT` などを書き換えず、EXP03側の実験結果保存関数も呼ばない。出力先はEXP04が自身の `EXP`、`OUT`、`FIG` 定数で管理する。

### 10.2 EXP04の直接import

| import | 実際の役割 |
|---|---|
| `from __future__ import annotations` | 型注釈の評価を遅延するPythonの機能 |
| `datetime as dt` | `metrics.json` に記録する実行日時 |
| `json` | 参照metricsと採用設定の読み込み |
| `nullcontext` | 既存のログオブジェクトを、追加のログ枠を作らずに共有する |
| `Path` | ソース位置からのルート探索と入出力パス操作 |
| `subprocess` | 公式validatorを別プロセスとして起動する |
| `sys` | 実行中と同じPythonを `sys.executable` でvalidatorへ指定する |
| `numpy as np` | 配列操作、マスク、到着側集計、結合、数値検査 |
| `EXP03_run as base` | モデル本体・Dataset・日付・設定・正規化処理を再利用する |
| `day_errors`, `score` | 日別誤差と期間平均の公式指標を計算する |
| `check_od_tsv`, `write_od_tsv` | 提出TSVの保存と検査 |
| `write_metrics`, `write_records_csv` | 評価値と地域別の表を保存する |
| `save_local_error_map` | 最終採用予測の空間的な誤差を描く |
| `municipality_error_rows`, `write_municipality_errors` | 起点・終点・対角の市町別誤差を集計する |
| `save_pred_dist` | 提出予測と検証予測の日次推移を描く |
| `run_log` | 全体・工程別の時間、結果、例外、成果物の記録 |

`candidate_plot()` 内では `matplotlib.pyplot`、`self_test()` 内では `types.SimpleNamespace` を関数内importする。前者は実装が保持している比較図の描画、後者は自己検査用の最小データオブジェクトに使う。

### 10.3 モデル本体側の依存

`EXP03_run.py` の学習処理では、特に次の依存が重要になる。

| 依存 | 役割 |
|---|---|
| `ast` | OD辞書表現を安全にパースする |
| `pandas` | 366日の日付軸、外部CSV、日付整列、気象データのpivot |
| `numpy` | 状態推定、分散更新、マスク付きの行列計算 |
| `scipy.linalg.eigh` | 共通回帰係数の線形方程式で使う安定した対称行列処理 |
| `dataclasses.dataclass` | `Prediction` の返り値を構造化する |
| `time` | モデル単位の所要時間を測る |
| `os` | `MPLCONFIGDIR` の既定値を設定する |
| `common.local_error_map.validation_days` | 検証日の定義を読み込む |
| `common.competition_metric` | 評価境界の定義をDataset構築に利用する |

同ファイルはEXP03全体の実行機能も持つため、JSON出力、ログ、描画などの共通関数もimportする。ただし、EXP04は `base.validate()`、`base.submit()`、`base.ablations()`、`base.residual_models()` を呼ばない。

CatBoostのimportは `residual_models()` の内部にある。最終EXP04ではこの関数を通らないため、`requirements.txt` にCatBoostが記載されていても、最終予測がCatBoostを使っていることにはならない。

### 10.4 importを解決する方法

リポジトリルートから

```bash
python3 src/EXP04_run.py
```

を実行すると、Pythonの探索パスの先頭にスクリプトのある `src/` が入る。そのため、同じディレクトリの `EXP03_run.py` と、`src/common/` 内の各モジュールを読み込める。

`ROOT = Path(__file__).resolve().parents[1]` により、データや成果物のパスはソースファイルを基準に決まる。コマンドライン引数でデータやモデルを切り替える実装にはなっていない。

## 11. EXP04の関数呼び出しと処理内容

### 11.1 全体の呼び出し関係

```text
EXP04_run.py の __main__
└── run()
    ├── run_log(...)
    ├── self_test()
    ├── validate(log)
    │   ├── check_reference()
    │   ├── base.Dataset(validation=True)
    │   │   └── load_features()
    │   ├── fit(data, CVの日付インデックス, log, "validate")
    │   │   ├── estimate(data, "D", DIAG_GROUPS, ...)
    │   │   ├── 出発側：estimate("O") → estimate("r") → base.normalize()
    │   │   └── 到着側：IncomingDataset(data)
    │   │       └── estimate("O") → estimate("r") → base.normalize()
    │   ├── reconstruct() → evaluate() → day_errors() → score()
    │   ├── select() → 最終採用はN-B
    │   └── metrics、地域別CSV、誤差図の保存
    └── submit(validation_predictions, log)
        ├── 保存済みの採用設定と参照設定を確認
        ├── base.Dataset(validation=False)
        ├── fit(data, TESTの日付インデックス, log, "submit")
        ├── 採用済みの1:1結合 → reconstruct(outside=True)
        ├── write_od_tsv() → check_od_tsv()
        ├── subprocess.run(...) で公式validator
        └── save_pred_dist() → ログ保存
```

`estimate()` の内側は次の順になる。

```text
base.fit_component()
├── 目的変数、学習マスク、有効系列を構築
├── data.features()
├── prepare_features()
├── Regression(...)
├── 回帰・Q・Rの初期化
├── EM反復
│   ├── smooth()：カルマンフィルター＋RTS
│   ├── Regression.fit()：回帰係数更新
│   └── Q・R更新、停止判定
├── 最終パラメータでsmooth()
├── 対角・総量のexpm1と非負化
└── Prediction(...) を返す
```

### 11.2 `specification()`：採用条件を機械的に記録する

外部特徴群、状態空間モデルの設定、結合重み、到着側特徴の反転方針、`catboost=False`、`fixed=False` を辞書として返す。

`fixed=False` は、時変の潜在水準を使う分岐を意味する。結合重みが固定かどうかを表す引数ではない。最終モデルでは、状態は時間とともに変化し、出発・到着の結合重みは0.5に固定される。

### 11.3 `check_reference()`：基盤モデルとの整合を検査する

`experiments/EXP03/metrics.json` を読み、対角・非対角の外部特徴群、残差補正の不使用、状態モデルの指定、共通設定がEXP04の前提と一致していることを確認する。

ここで読み込むのは設定と評価値であり、EXP03の学習済み重みではない。EXP04は、その時点の学習データから状態・係数・分散を改めて推定する。

検証後には、出発側の再現値を小数6桁へ丸めてEXP03の保存値と照合する。保存精度に合わせた整合検査であり、丸め前の実数が完全一致することを要求しているわけではない。

### 11.4 `IncomingDataset`：同じODを到着側で再集計する

実装の中心は次の処理である。

```python
self.__dict__ = source.__dict__.copy()
self.pairs = [(d, o) for o, d in source.pairs]
self.origin = source.dest.copy()
self.dest = source.origin.copy()
self.total = np.zeros_like(source.total)
for p, destination in enumerate(self.origin):
    self.total[:, destination] += self.flow[:, p]
```

`__dict__` は浅いコピーであり、元の `flow` など読み取り専用として使う配列を共有する。一方、`origin`、`dest`、集計後の `total`、`ratio` はビュー側で用意する。

特に重要なのは、**`flow[:, p]` の列順と値を変えない**ことである。列 `p` は最後まで元の有向OD $o\rightarrow d$ に対応する。役割を反転した `origin` で同じ列を集計することにより、共通学習コードの「出発側総量」が物理的な流入総量になる。

たとえば、ある日に `a→b=2`、`c→b=4` なら、到着側での `b` の総量は6、元の2つのODの配分はそれぞれ1/3と2/3になる。`b→a` や `b→c` の観測値を探して置き換える処理はない。

外部特徴も `origin`、`dest` によって参照するため、総量の集約先と特徴の役割が一緒に反転する。こうして到着側専用の別モデル実装を重複して書かず、同じ `fit_component()` と `normalize()` を再利用する。

### 11.5 `fit()` と内部の `estimate()`

`estimate()` は工程名をログへ記録し、次の呼び出しで1モデルを学習する。

```python
prediction = base.fit_component(
    view, kind, groups, False, data.observed
)
```

`prediction.diagnostics` を診断一覧へ追加し、`prediction.mean[times]` で必要な予測日だけを取り出す。状態モデル自体は366日全体を処理し、抽出だけを `times` で行う。

対角を1回推定した後、`("F", data)` と `("R", IncomingDataset(data))` を順に処理する。それぞれ総量と配分を推定し、正規化した配分を用いて

```python
flows[name] = total[:, view.origin] * share
```

と非対角を復元する。両側で列順を維持しているため、`flows["F"]` と `flows["R"]` は、そのまま同じ位置同士を平均できる。

`Prediction` は平均、分散、マスク、診断、係数一覧、特徴量監査を返すが、EXP04でそのまま永続保存するのは主にCVの診断である。学習済み係数のチェックポイントを読み込んで提出する構成にはなっていない。

### 11.6 `validate()`：学習、採用、記録

`validate()` はCVを隠したDatasetを作り、5モデルを学習して検証日を予測する。コード上は出発側、到着側、等重み結合を採点する選択処理を持ち、保存された最終採用結果は `N-B` である。

`select()` は14日全体のCombined NRMSEを基準にする。差が `1e-12` 以下なら更新せず、実装順により同点優先は出発側、到着側、等重み結合となる。最終提出の重みは、この選択結果を `metrics.json` から読み直して決める。

本書の方法・結果表は保存済みの最終採用構成を対象にしている。一方、コードの再実行は固定重みの推論だけでなく、この検証・選択処理も含む。入力や設定が変われば、選択結果が変わる可能性はある。

### 11.7 `submit()`：全観測で学習し直す

`submit()` は保存された設定が現在の `specification()` と一致することを確かめ、`Dataset(False)` を作る。この段階ではCV日を正解の隔離対象から外し、全292観測日で5モデルを最初から学習し直す。

- テスト日は `observed=False` であることをassertする。
- CV14日は学習観測へ戻っていることをassertする。
- 候補OD集合も再構築するため、このデータでは530ペアになる。
- 特徴の補完平均・標準偏差、配分のフォールバック、評価外の曜日平均も再計算する。
- 採用した重み0.5で非対角を結合し、58日分を復元する。

したがって、CV用に学習した状態や回帰係数を、そのまま提出用へ流用しているわけではない。

### 11.8 自己検査・形式検査・ログ

`self_test()` は、3本の非対称な有向ODを持つ小さな例で、到着側集計、元配列の保持、2回反転したときの復元、ゼロ総量時のフォールバックを確認する。予測精度のテストではなく、方向を取り違えないための数値実装の検査である。

提出TSVは `write_od_tsv()` が日付順に、ヘッダーなしの2列で書き出す。`check_od_tsv()` は日付集合、辞書構造、ID、有限かつ非負の値を検査する。EXP04からは日次総量の期待値を渡さないため、一定総量への一致検査は行わない。

続いて、`src/common/humob2026_validator.py` が存在すれば次の形で起動する。

```python
subprocess.run(
    [sys.executable, str(official_validator), str(path)],
    check=True, capture_output=True, text=True
)
```

現在のローカル環境にはこのvalidatorがあり、保存済み成功ログにも `Validation passed!` が記録されている。ファイルがない環境では、コードは内部検査のみ通過したことを明示する。validatorが確認するのは提出形式であり、非公開正解に対する予測精度ではない。

`run_log()` は `with` の終了時に実行履歴を追記する。例外時も失敗した工程と例外を記録し、例外を再送出する。EXP04のスクリプトが行うのは提出ファイルの生成までで、コンペサイトへのアップロードは含まれない。

## 12. 評価指標と最終モデルの診断

### 12.1 公式指標に合わせた計算

[competition_metric.py](src/common/competition_metric.py) の `day_errors()` は、日ごとに対角・非対角の二乗誤差を別々に集める。評価領域のセル数を $M$ とすると、

$$
\mathrm{RMSE}_{D,t}
=\sqrt{\frac{1}{M}\sum_o(\widehat Y_{t,o,o}-Y_{t,o,o})^2}
$$

$$
\mathrm{RMSE}_{N,t}
=\sqrt{\frac{1}{M(M-1)}\sum_{o\ne d}(\widehat Y_{t,o,d}-Y_{t,o,d})^2}
$$

である。和の対象は評価領域の全ペアであり、辞書にないペアの値は0。非対角の分母を、モデル化した525候補だけに縮めることはしない。

次に、`score()` が日次RMSEを対象日数で平均し、主催者指定の固定RMSで正規化する。

$$
\begin{aligned}
\mathrm{NRMSE}_D &=
\frac{1}{C_D\,|\mathcal V|}
\sum_{t\in\mathcal V}\mathrm{RMSE}_{D,t}, \\
\mathrm{NRMSE}_N &=
\frac{1}{C_N\,|\mathcal V|}
\sum_{t\in\mathcal V}\mathrm{RMSE}_{N,t}.
\end{aligned}
$$

$$
\mathrm{Combined\ NRMSE}
= \frac{\mathrm{NRMSE}_D+\mathrm{NRMSE}_N}{2}.
$$

$C_D,C_N$ は共通採点モジュールに定義された正規化定数を使う。検証窓ごとに正解平均を計算して置き換える処理はしない。また、全日・全ペアの二乗誤差を一度に平均してから平方根を取る計算とも異なる。**日ごとに平方根を取った後で平均する**ことが再現上重要である。

学習で直接最適化するのは、このCombined NRMSEそのものではない。対数変換した対角・総量と元尺度の配分について、正則化付きガウス状態空間モデルを学習し、復元したODを公式指標で評価する。目的関数と評価指標にはこの違いがある。

### 12.2 最終構成のCV学習診断

以下は `results.fit_diagnostics` に保存された値であり、**CV時の学習**を示す。`Q` は状態ノイズ分散、`R` は観測ノイズ分散である。

| モデル | 有効系列数 | 学習観測要素数 | Q | R | 反復数 | 収束判定 |
|---|---:|---:|---:|---:|---:|---|
| 対角 `stage0-D5` | 911 | 253,258 | 0.000383 | 0.105194 | 200 | 未達 |
| 出発側総量 `stage1-F-total` | 143 | 39,754 | 0.000284 | 0.051926 | 200 | 未達 |
| 出発側配分 `stage1-F-share` | 525 | 86,448 | 0.000114 | 0.014972 | 200 | 未達 |
| 到着側総量 `stage2-R-total` | 148 | 41,144 | 0.000244 | 0.050673 | 200 | 未達 |
| 到着側配分 `stage2-R-share` | 525 | 84,547 | 0.000119 | 0.014804 | 200 | 未達 |

有効系列数911は、評価対象セルが911しかないという意味ではない。対角1,476セルのうち、CVの学習期間で正の値を持つ系列が911あり、残りを0としたという意味である。

総量で出発側143・到着側148と異なるのは、流出のあるセル集合と流入のあるセル集合が一致するとは限らないためである。配分モデルの学習観測要素数が `525 × 278` より小さいのは、分母総量が0の日を学習から除外するためである。

対角・総量の $Q,R$ は対数変換後の尺度、配分の $Q,R$ は比率の尺度での値であり、大きさをそのまま異種モデル間で比較しない。

### 12.3 非対角誤差の内訳

最終 `N-B` の14日間の非対角二乗誤差を、辞書への出現関係で分けた結果は次のとおり。

| 分類 | 意味 | SSE | 非対角SSEに占める割合 |
|---|---|---:|---:|
| `hit` | 正解・予測の両方にあるODの値の誤差 | 672.864627 | 71.029394% |
| `miss` | 正解にあり、予測にはないODの誤差 | 19.453909 | 2.053607% |
| `extra` | 予測にあり、正解にはないODの誤差 | 254.985944 | 26.917000% |

`hit` は「正解できた」という意味ではなく、両方にODが存在する場合の誤差である。大半のSSEはこの部分にあり、既存候補の流量をどこまで正確に推定できるかが重要になる。また、`extra` の寄与も残っており、学習期間に見られたODが毎日出現するとは限らないことが分かる。

この割合はSSEの内訳であり、Combined NRMSEへの加算可能な寄与率ではない。公式指標には平方根・日次平均・成分別正規化が入るためである。

### 12.4 出発・到着予測の関係

候補OD525本×CV14日の7,350要素における、出発側と到着側の誤差相関は **0.992131**、予測値の平均絶対差は **0.025955** だった。

両モデルは同じOD観測と同じ特徴群を基にするため、強く相関している。一方で、総量の集約方法と特徴の役割が異なるため、予測は完全には一致しない。等重み平均は、その差を取り込んだ最終予測になる。この相関の計算対象は候補OD内であり、評価領域の全非対角ペアではない。

### 12.5 地域別・日次の診断出力

最終採用モデルの地域別誤差は [municipality_errors.csv](experiments/EXP04/outputs/municipality_errors.csv) に保存される。対角はセルの担当市町、非対角は出発セルの担当市町と到着セルの担当市町の2通りで集計する。出発側集計と到着側集計は同じ誤差を別の軸で見たものなので、両方を加算しない。

また、[local_error_map.png](experiments/EXP04/figures/local_error_map.png) はセル別誤差の位置を、[EXP04_pred_dist.png](experiments/EXP04/figures/EXP04_pred_dist.png) は評価領域内の対角・非対角の予測日次推移を確認する成果物である。地域のSSEが大きいことだけで、その地域の相対的な予測精度が悪いとは判断できない。地域別の流量規模も合わせて解釈する必要がある。

## 13. 再現に必要な設定と実行方法

### 13.1 最終モデルのハイパーパラメータ

| 実装上の設定 | 値 | 用途 |
|---|---:|---|
| `em_max_iter` | 200 | EM反復の上限 |
| `em_tol` | `1e-5` | 目的関数の相対的な停止判定 |
| `variance_floor` | `1e-8` | Q・Rの下限 |
| `initial_variance` | `1e4` | 初日・地震リセット時の事前分散 |
| `weekday_ridge` | 1.0 | 系列別カレンダー係数のL2罰則 |
| `global_ridge` | 1.0 | 共通外部特徴係数のL2罰則 |
| `deviation_ridge` | 20.0 | 対角のセル別外部特徴偏差のL2罰則 |
| `inverse_log` | `median_expm1` | 対角・総量の逆変換方針 |
| `fixed` | `False` | 時変の局所水準を使う |
| `catboost` | `False` | 最終モデルに残差補正を追加しない |
| 到着側の結合重み | 0.5 | 出発側0.5との等重み平均 |

設定辞書には `crossfit_folds=5`、`crossfit_block_days=28`、`seed=2026` も記録される。これらは共通設定に残る値であり、最終EXP04の処理は内部クロスフィットや乱数によるサンプリングを実行しない。したがって、これらを最終モデルのCV回数や確率的な学習設定として説明しない。

### 13.2 実行環境

[README.md](README.md) には実験時のPythonが3.9.6と記録され、[requirements.txt](requirements.txt) は次のバージョンを指定している。

```text
numpy==2.0.2
scipy==1.13.1
pandas==2.3.3
matplotlib==3.9.4
catboost==1.2.10
```

最終EXP04の主要計算はNumPy・SciPyによるCPUの配列演算で、GPUを要求する処理はない。CatBoostはリポジトリ全体の依存であり、前述のとおり最終学習経路では呼ばれない。

### 13.3 入力を用意する順序

再現には配布ODのほか、外部資料から作成した加工済みCSVと、参照するEXP03のmetricsが必要である。

1. 配布TSVと公開資料の原本を所定の位置へ配置する。
2. セル―市町対応を作成する。
3. 被害情報PDFから避難者数・断水戸数を抽出し、率のCSVを作成する。
4. 施設別避難所情報をセルごとに集計する。
5. 気象CSVをモデル入力形式へ変換する。
6. 参照するEXP03の設定・metricsと、EXP04の採用設定が整合していることを確認する。
7. EXP04を実行する。

前処理スクリプトの実行順と入力位置は [前処理README](src/preprocessing/README.md) に記載されている。PDFの文字抽出にはPopplerの `pdftotext` を使う。EXP04単体で原本のダウンロードや前処理からすべてを始める構成にはなっていない。

### 13.4 実行コマンドと生成物

リポジトリルートで実行する。

```bash
python3 src/EXP04_run.py
```

このコマンドは、自己検査、CVの学習・評価、採用設定の保存、全観測での再学習、58日分の提出TSVの生成、形式検査、診断図の保存までを行う。実行するとEXP04の数値・出力・図を更新し、実行ログを追記する。

最終的に提出するファイルは [experiments/EXP04/outputs/submission.tsv](experiments/EXP04/outputs/submission.tsv) である。形式を説明するための例は次のとおりで、以下の値は実測結果ではない。

```text
20240201<TAB>{'50_40': {'50_40': 12.3, '51_40': 0.4}}
```

実際のファイルでは `<TAB>` の位置にタブ文字が入り、58日分の各行にその日のOD辞書が入る。

### 13.5 計算時間と記録の範囲

[実行ログ](experiments/EXP04/.log) の2026-09-21 00:43:47開始の成功実行では、全体所要時間は **121.8秒** だった。これは検証用5モデルと提出用5モデルの学習、読み込み、採点、保存、検査、図の生成を含む。その実行のハードウェア仕様は、このログからは特定できない。

`metrics.json` の `fit_diagnostics` は検証側の5モデルを記録する。提出側も再学習するが、その詳細診断一式は同じ配列に追記されない。提出側の到達反復数や係数が、検証側の診断値と同一だったと仮定してはならない。

また、EXP04は学習済み係数・全状態をモデルファイルとして保存していない。提出物を再生成するには、入力データと実装・設定を揃えて再学習する必要がある。

## 14. 手法の適用範囲と解釈

本手法の特徴は、災害後のOD補完を、**時変の水準・公開外部情報・出発側と到着側の集約**に分けて実装した点にある。対角・総量では値の尺度を `log1p` で圧縮し、疎な非対角を総量と配分に分け、前後の観測から欠損期間の潜在水準を平滑化する。最後に、同じ有向ODに対する出発側・到着側の推定を平均して提出値とする。

結果を解釈する際の主な制約は以下である。

- **評価期間**：掲載数値は1月末・4月初めの14日間であり、2〜3月の非公開正解に対する実測値ではない。
- **数値収束**：CVの全モデルで200反復の上限に達しており、停止基準を満たした推定とは言えない。
- **未知OD**：学習期間に一度も正の値がないODには正の予測を新規生成しない。
- **情報の時点**：4月以降のOD、対象期間の気象、事後的に整理した公開資料を使う、オフラインの補完設定である。
- **因果解釈**：避難・断水・避難所に対応する係数は予測のための関係を表し、介入による因果効果を識別した値ではない。
- **保存範囲**：CVのスコア・診断と提出TSVは残るが、係数・全状態・すべての特徴監査が永続保存されるわけではない。

以上を踏まえ、EXP04の最終手法は、**外部特徴付き局所水準状態空間モデルで対角・流量総量・配分を推定し、非対角ODを出発側と到着側から等重みで結合する方法**と要約できる。最終モデルのローカル検証値は、1月末 **0.205621**、4月初め **0.196006**、14日全体 **0.200814** である。

## 参照した実装・記録

- [コンペの課題・配布形式・評価方法](Competition.md)
- [EXP04の実行・到着側ビュー・結合・提出処理](src/EXP04_run.py)
- [Dataset・特徴生成・状態空間モデル・EM・正規化の実装](src/EXP03_run.py)
- [最終構成と評価数値の正本](experiments/EXP04/metrics.json)
- [実行時の結果と考察](experiments/EXP04/report.md)
- [実行時間・提出検査の記録](experiments/EXP04/.log)
- [公式評価式のローカル実装](src/common/competition_metric.py)
- [TSV出力・内部形式検査](src/common/experiment_io.py)
- [配布された提出形式validator](src/common/humob2026_validator.py)
- [外部データの前処理と依存関係](src/preprocessing/README.md)
- [公開被害情報・人口・行政区域の出典記録](data/processed/events/README.md)
- [気象データの加工記録](data/processed/weather/README.md)
- [避難所施設データの加工記録](data/processed/避難所/README.md)
